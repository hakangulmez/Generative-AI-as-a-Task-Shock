# API keys for thesis pipeline. Copy this file to .env and fill in real values.
# .env is gitignored; never commit it.

# Primary scoring (Claude Haiku 4.5 via Anthropic)
ANTHROPIC_API_KEY=

# Cross-LLM validation — optional, add when ready
OPENAI_API_KEY=
OPENROUTER_API_KEY=
GOOGLE_AI_STUDIO_KEY=
GROQ_API_KEY=
